{
  "name": "TAF Short",
  "version": "1.0.0",
  "description": "TAF Short: Terminal aerodrome forecast (up to 12 hours)",
  "extends": "taf",
  "identifier": "TAF",
  "suggestions": {
    "items": {
      "validityPeriod": [
        {
          "text": "0000/0000",
          "description": "Validity period DDHH/DDHH (max 12h)",
          "editable": [
            { "start": 0, "end": 2 },
            { "start": 2, "end": 4 },
            { "start": 5, "end": 7 },
            { "start": 7, "end": 9 }
          ],
          "defaultsFunction": "() => { const now = new Date(); const d = String(now.getUTCDate()).padStart(2, '0'); const h = String(now.getUTCHours()).padStart(2, '0'); const addHours = (hours) => { const future = new Date(now.getTime() + hours * 60 * 60 * 1000); return String(future.getUTCDate()).padStart(2, '0') + String(future.getUTCHours()).padStart(2, '0'); }; return [{ text: d + h + '/' + addHours(6), description: '6 hours validity' }, { text: d + h + '/' + addHours(9), description: '9 hours validity' }, { text: d + h + '/' + addHours(12), description: '12 hours validity' }]; }"
        }
      ]
    },
    "after": {
      "issueTime": [
        "validityPeriod"
      ]
    }
  },
  "tokens": {
    "validityPeriod": {
      "pattern": "^\\d{4}\\/\\d{4}$",
      "style": "datetime",
      "description": "Validity period Y1Y1G1G1/Y2Y2G2G2 (max 12h)",
      "validator": "DDHH/DDHH-short"
    }
  }
}
